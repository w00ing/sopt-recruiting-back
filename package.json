{
  "name": "sopt-recruiting",
  "version": "1.0.0",
  "scripts": {
    "prepare": "cd src/functions && npm install",
    "postprepare": "cd ../..",
    "dev": "cross-env NODE_ENV=development firebase serve --only functions:api --port=9000",
    "deploy": "cross-env NODE_ENV=production firebase deploy --only functions:api"
  },
  "dependencies": {
    "@notionhq/client": "^0.3.0",
    "cross-env": "^7.0.3"
  },
  "devDependencies": {
    "eslint": "^7.28.0",
    "eslint-config-prettier": "^8.3.0",
    "nodemon": "^2.0.7",
    "prettier": "^2.3.1"
  },
  "keywords": [
    "Node.js"
  ]
}
